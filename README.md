# Week2_DAY4
In Python, you can manage student records using file operations. First, create and write a file (w) to store structured data like roll number, name, and marks. Then, append new records (a) without deleting existing ones. Finally, read the file (r) to verify or display the stored information.
